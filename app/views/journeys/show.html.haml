%label{ :style => "text-decoration: underline;"}
  Name:
%p  
  = @journey.name
%br/
%label{ :style => "text-decoration: underline;"}
  Leaving Time:
%strong
  = @journey.leaving_time
%br/

%h4{ :style => "color: grey;"}
  Routes
%table.table.table-dark
  %thead
    %tr
      %th{:scope => "col"} Departure
      %th{:scope => "col"} Arrival
      %th{:scope => "col"} Arrival time Aprox.
  %tbody
  - @journey.routes.each do |route|
    %tr
      %td= route.init_location.address
      %td= route.end_location.address
      %td= route.arrival_time

= link_to "Back", journeys_path